{
  "name": "hms-email-service-python",
  "version": "1.0.0",
  "description": "Python serverless email service for HMS",
  "scripts": {
    "offline": "serverless offline start --port 3000",
    "deploy": "serverless deploy"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4",
    "serverless-python-requirements": "^6.0.0"
  }
}

